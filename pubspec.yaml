name: examApp
description: A new Flutter project

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.2
  http: ^1.2.2
  shared_preferences: ^2.5.4
  provider: ^6.1.5+1
  fluttertoast: ^9.0.0
  screenshot: ^3.0.0
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true